name: file_picker_example
description: An example of how to use the file_picker plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.10.0 <4.0.0"

formatter:
  page_width: 80

dependencies:
  flutter:
    sdk: flutter
  file_picker:
    path: ../
  file: ^7.0.1

dependency_overrides:
  flutter_plugin_android_lifecycle:
    path: /Users/danieljimene/developer/flutter/flutter_packages/packages/packages/flutter_plugin_android_lifecycle

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true